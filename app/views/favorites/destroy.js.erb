// change the full favorite star icon to an empty star icon
$('.fave_buttons_<%= @hack.id %>').html('<%= link_to favorites_path(@hack), method: :post, remote: true, id: "fave_button" do %><span class="glyphicon glyphicon-star-empty yellow_h"></span><% end %>')
