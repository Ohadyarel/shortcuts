// update the up/down vote counters when a vote is casted
$('#downvote_count_<%= @hack.id %>').html('<%= @hack.get_downvotes.size %>')
$('#upvote_count_<%= @hack.id %>').html('<%= @hack.get_upvotes.size %>')